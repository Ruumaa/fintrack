import 'package:flutter/material.dart';

class AppColor {
  // Warna Primer (lebih bersih dan modern)
  static const primary = Color(0xFF5A68E0);
  // Warna Sekunder (Aksen)
  static const secondary = Color(0xFF00C896);
  // Latar Belakang Aplikasi (sangat terang untuk kontras)
  static const background = Color(0xFFF9FAFB);
  // Teks Utama (dark blue-grey)
  static const textPrimary = Color(0xFF1E293B);
  // Teks Sekunder (medium grey)
  static const textSecondary = Color(0xFF64748B);
  // Warna untuk Error/Peringatan
  static const error = Color(0xFFEF4444);
  // Warna Putih Murni
  static const white = Colors.white;
}
